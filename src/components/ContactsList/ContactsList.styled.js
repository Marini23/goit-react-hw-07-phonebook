import styled from 'styled-components';

export const ListContacts = styled.ul`
  width: 500px;
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-left: 30px;
  padding: 0;
  list-style: inside;
`;
